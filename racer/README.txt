Math Racer 3D (Clean Rewrite)

Run locally (recommended):
  python3 -m http.server 8000
Open:
  http://localhost:8000

Notes:
- Overlays (Menu/Garage/Stats) are ALWAYS clickable: when open, gameplay input layer is disabled via body.overlayOpen.
- Close buttons use pointerdown for reliability.
